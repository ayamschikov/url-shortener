DATABASE_URL=postgres://postgres:postgres@localhost:5423/urlshortener?sslmode=disable
REDIS_URL=redis://localhost:6397
PORT=8080
# Лимит запросов на IP в минуту (по умолчанию 100). Для нагрузочного теста
# (make load) поставь высокий, иначе быстро упрёшься в 429.
RATE_LIMIT_PER_MIN=100000
